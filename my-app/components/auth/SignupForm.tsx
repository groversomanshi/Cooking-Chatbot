"use client";

import { Alert } from "@mui/material";

export default function SignupForm() {
  return (
    <Alert severity="info">
      Account creation will happen through Google OAuth once credentials are
      configured.
    </Alert>
  );
}
